input {
   kafka {
	   topics => ["slack","slack_test"]
	   bootstrap_servers => "kafka:9092"
	   decorate_events => "true"
    }	
}

## Add your filters / logstash plugins configuration here

output {
	if [@metadata][kafka][topic] == "slack" {
		elasticsearch {
			hosts => "elasticsearch:9200"
			user => "elastic"
			password => "changeme"
			index => "slack"
		}
	} else {
		elasticsearch {
			hosts => "elasticsearch:9200"
			user => "elastic"
			password => "changeme"
			index => "slack_test"
		}
	}

}